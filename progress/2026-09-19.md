# Progress — Sat 2026-09-19

Goal: round 1 = hand-type every HTML/CSS example from the review plan and course pack. Exam simulation only after round 1.
Test 1: Thu Oct 1, 2026 (handwritten, no tech, covers Sep 1–29).

## Done today

**HTML (Chapter 3 examples): all typed**
- [x] H1 skeleton, meta, old-style `font` / `bgcolor`
- [x] H2 links, image, jump anchor
- [x] H3 iframe and link targets
- [x] H4 table with colspan / rowspan (`table.html`)
- [x] H5 HTML4 table sizing (5 tables, `H5_table-custom.html`)

**CSS (Chapter 4 examples): all typed**
- [x] B1 text, colour, font, background
- [x] B2 inline styles, `display` values
- [x] B3 internal stylesheet, inline override
- [x] B4 classes, two classes on one element, grid vs flex
- [x] B5 external stylesheet (`B5_styles.css`, `B5_demo.css`)
- [x] B6 box model, centred div, circle, border / margin / padding snippets
- [x] B7 two columns: flex, float (+ clearfix), grid
- [x] B8 full page float layout + media query
- [x] B9 hover, transition, `position: absolute`, z-index
- [x] B10 static vs liquid table
- [x] B11 responsive images, `<picture>`, `vw`, video
- [x] B12 flex-wrap + media query, float nav bar, drop cap, gallery, floated divs, flex tour
- [x] B13 CSS grid: Chania (named areas), 12-column grid
- [x] B14 Grid View "Question": 12-col floats, flexbox, CSS grid

## Concepts worked through (asked / explained)
- `ul` needs `li` children; `li` gets the bullet from its `ul` parent
- `<p>` for long text, `<span>` for inline pieces, `<h2>` is a heading
- `hspace` / `vspace` on `<img>`
- `.back gridlike`: several classes on one element, class order doesn't matter
- `display: grid`, `grid-template-columns`, `fr`, `repeat()`
- `flex-wrap`, the `flex` shorthand (grow shrink basis)
- `.container::after` clearfix; when float needs clearing (clearfix, `overflow: hidden`, `clear: both`)
- `:hover` base rule + override, `cursor: pointer`, `transition` on the base rule
- `position: absolute` (placed element vs positioned ancestor)
- `border-collapse: collapse`
- `vw` measures font size, not text width
- child selector `>` vs descendant (space)
- `min-width` vs `max-width`, and why media query order matters
- `[class*="col-"]` attribute selector; what can float

## Typos found in my own files (fixed unless noted)
- B4 `grid-templet-columns` — fixed
- B10 `800xp`, missing `<table>` wrapper — fixed
- B13 stray `*/` before `.header > h1` — fixed
- B14 flex: boxes used `col-3` instead of `col` — fixed
- B9 `background-color 0.3 ease` needs `0.3s` — fixed
- B12 gallery `mergin: 0` needs `margin` — fixed

## Extras
- Saved reference copies in `practice/slidesample/`: selector sampler (`B15`), slide 7 floats, slide 14 media query (`B16`), HTML5 semantic layout (`B17`).
- Git set up in `D:\comp307`, remote `origin` = `Oceanlord936/comp307-sandbox`. `review_plan.md` is git-ignored.

## Still to do for round 1
- [ ] Type your own version of the four `slidesample` files (B15, B16 x2, B17), then compare with the reference copies
- [ ] `B17_div.html`: copy of B17 with `header` / `section` / `nav` / `article` / `footer` replaced by `<div>`
- [x] Fix the two open typos above

## Next
- Round 2: exam simulation (ask for it once round 1 is finished)
